import { DuiFormaterPipe } from './dui-formater.pipe';

describe('DuiFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new DuiFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
